# Namecheap Private Email — never commit a real password.
# Copy to .env.local for local experiments. Railway should set these
# on the eli-outreach service, not in this repo.

SEND_ENABLED=false
SMTP_HOST=mail.privateemail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=max@elbertalogistics.net
SMTP_FROM=max@elbertalogistics.net
SMTP_PASS=
